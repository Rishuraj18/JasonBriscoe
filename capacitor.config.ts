import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'YourAppName',
  webDir: 'build',  // Change this if needed
  bundledWebRuntime: false
};

export default config;